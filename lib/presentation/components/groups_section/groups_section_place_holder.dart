import 'package:flutter/material.dart';

class GroupsSectionPlaceHolder extends StatelessWidget {
  const GroupsSectionPlaceHolder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}