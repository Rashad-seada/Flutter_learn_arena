

import 'package:flutter/material.dart';

class NotificationSectionPlaceHolder extends StatelessWidget {
  const NotificationSectionPlaceHolder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}