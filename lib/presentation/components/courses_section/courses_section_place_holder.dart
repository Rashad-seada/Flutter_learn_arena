import 'package:flutter/material.dart';

class CourseSectionPlaceHolder extends StatelessWidget {
  const CourseSectionPlaceHolder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}