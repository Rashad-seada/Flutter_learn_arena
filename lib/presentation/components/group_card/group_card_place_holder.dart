import 'package:flutter/material.dart';

class GroupCardPlaceHolder extends StatelessWidget {
  const GroupCardPlaceHolder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}